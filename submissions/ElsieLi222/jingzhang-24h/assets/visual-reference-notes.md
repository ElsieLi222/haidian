# Offline visual references

DRAWING TODO / DATA TODO. Only local files under `visual/assets/` may be referenced by `visual/index.html`; no CDN, external fonts, remote map tiles, external scripts, APIs, or iframes.

Future references must preserve the 24H narrative sequence and point to registered drawings D01–D08 or locally derived evidence figures. Provisional boundaries must remain explicitly labeled and must never be represented as official boundaries.
